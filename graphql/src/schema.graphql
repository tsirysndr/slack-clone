### This file was generated by Nexus Schema
### Do not make changes to this file directly


"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

type Mutation {
  createChannel(isPrivate: Boolean, name: String!): channel
  editChannel(id: String!, isPrivate: Boolean, name: String): channel
  editProfile(firstName: String, id: String!, lastName: String, password: String, username: String): user
  login(password: String!, username: String!): user
  sendMessage(content: String!, recipient: String!): message
}

type Query {
  allChannels: [channel]!
  allMessages: [message]!
  allUsers: [user]!
  findChannel(id: String!): channel
  findMessage(id: String!): message
  findUser(id: String!): user
}

type Subscription {
  newMessage(id: String!): message
  userOnlineStatus(id: String!): user
}

enum UserStatusEnum {
  ACTIVE
  AWAY
}

type channel {
  createdAt: DateTime!
  creator: user!
  creatorId: String!
  id: ID!
  isPrivate: Boolean!
  messages: [message!]!
  name: String!
  updatedAt: DateTime!
}

type message {
  channel: channel
  channelId: String
  content: String!
  createdAt: DateTime!
  id: ID!
  receiver: user!
  receiverId: String!
  sender: user!
  senderId: String!
  updatedAt: DateTime!
}

type user {
  channels: [channel!]!
  createdAt: DateTime!
  email: String!
  firstName: String
  id: ID!
  lastName: String
  received_messages: [message!]!
  sent_messages: [message!]!
  status: UserStatusEnum
  token: String
  updatedAt: DateTime!
  username: String!
}
